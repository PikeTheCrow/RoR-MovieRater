# RoR-MovieRater
A Movie rate and search platform built with Ruby and Rails 4

To run the app on your local server

```
	> git clone 
	> cd MovieApp
	> bundle install
	> rake db:migrate
	> rails s
```
